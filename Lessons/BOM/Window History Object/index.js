/*
    * BOM [Browser Object Model]:
        - History API:
            --- Properties
            ------ length => length of history pages;
            --- Methods
            ------ back() => A step backward;
            ------ forward() => A step forward;
            ------ go(Delta) => Position In History:
                ---------- go(2) => 2 steps forward;
                ---------- go(-3) => 3 steps backward;

    * Search [For Advanced Knowledge]:
        - pushState() + replaceState()
*/

console.log(history);